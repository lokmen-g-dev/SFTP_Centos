The script in "script_serveur_sftp.sh" is to create three users, the first is the sftp server user and the others are a simple user. As well as installing and configuring server File Transfer.
The script in "script_publique_key" to gener the public key of any user.
